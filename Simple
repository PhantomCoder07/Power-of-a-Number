#include <stdio.h>
#include <math.h>
int main()
{
    int base,exp,result;
    printf ("Enter the number: ");
    scanf ("%d",&base);
    printf ("Enter the power: ");
    scanf ("%d",&exp);
    result = pow(base,exp);
    printf ("Result of %d^%d = %d\n",base,exp,result);
    return 0;
}
